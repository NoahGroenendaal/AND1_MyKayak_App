<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mykayak_v2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SettingsActivity"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".FirebaseUIActivity" />
        <activity
            android:name=".CompareFragment"
            android:parentActivityName=".KayakPage" />
        <activity
            android:name=".ShoppingList"
            android:parentActivityName=".KayakPage" />
        <activity
            android:name=".Pyranha"
            android:parentActivityName=".KayakPage" />
        <activity
            android:name=".KayakPage"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>